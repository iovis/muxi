---@type muxi.Config
return {
  -- muxi_prefix = "g",
  -- tmux_prefix = true,
  -- uppercase_overrides = true,
  -- use_current_pane_path = false,

  plugins = {
    -- "tmux-plugins/tmux-resurrect",
  },

  fzf = {
    -- input = true,
    -- bind_sessions = false,
    -- args = { "--color=input-border:black" },
  },

  bindings = {
    -- e = {
    --   command = "muxi sessions edit",
    --   popup = { title = " sessions " },
    -- },
  },
}
